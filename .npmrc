# Node.js deprecation warnings suppression for development
# These warnings are from npm dependencies, not from our code
# They don't affect extension functionality

# Allow scripts to run (needed for esbuild postinstall)
allow-scripts=true

# Suppress specific deprecation warnings that come from dev dependencies
# DEP0169: url.parse() - from glob package
# These should be fixed in future npm/node versions
